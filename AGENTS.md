## Agent skills

### Issue tracker

Issues live in GitHub Issues (managed via `gh` CLI). See [issue-tracker.md](docs/agents/issue-tracker.md).

### Triage labels

Default triage label mapping is enabled (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See [triage-labels.md](docs/agents/triage-labels.md).

### Domain docs

Single-context repo layout (`CONTEXT.md` + `docs/adr/`). See [domain.md](docs/agents/domain.md).
